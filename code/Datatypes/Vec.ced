module Vec.

import Nat.
import Id.
import IIdMapping.
import IFixIndM.
import VecF.


Vec ◂ ★ ➔ Nat ➔ ★ = λ A : ★. λ n : Nat. IFixIndM · Nat · (VecF · A) (imapV · A) n.

rebVec ◂ ∀ A : ★. ∀ X : Nat ➔ ★. ∀ n : Nat. Π z : VecF · A · X n. {z nilVF consVF ≃ z}
 = Λ A. Λ X. Λ n. λ z. (VecFInd · A · X -n z) 
 · (λ q : Nat. λ z : VecF · A · X q. {z nilVF consVF ≃ z}) (Λ n'. λ a. λ x. β) β.
