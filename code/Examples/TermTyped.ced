import Ctx.
import Raw.
import Term.
import Id.
import Sigma.
import Typed.
module TermTyped (t2r : ∀ GA : CtxTp. Id · (Term GA) · Raw).

termTyped ◂ Π GA : CtxTp. Π t : Term GA. Typed GA (elimId · (Term GA) · Raw (t2r -GA) t)
  = λ GA. λ t. pair · (Term GA) · (λ t' : Term GA. {t' ≃ t}) t β.

